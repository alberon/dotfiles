#!/bin/bash
name="$1"

if [ -z "$name" ]; then
    echo "Usage: git init-github <name>" >&2
    exit 1
fi

git remote add origin "git@github.com:davejamesmiller/$name.git"
git branch --set-upstream master origin/master
git push --all
