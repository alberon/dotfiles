#!/bin/bash

# Grep for a given class definition in the source code
#
# e.g.
#   git cls MyClass
#
# is equivalent to:
#   git grep 'class MyClass'
#
# (but easier to type)

if [ -z "$1" ]; then
    echo "Usage: git grep <class name>" >&2
    exit 1
fi

exec git grep "class $1"
