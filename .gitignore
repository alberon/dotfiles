# Ignore everything in the root except the specified files
/*
!/.bash
!/.bash_completion
!/.bash_completion.d
!/.bin
!/.dotfiles
!/.drush
!/.elinks
!/.gitattributes-global
!/.gitconfig
!/.github
!/.gitignore
!/.gitignore-global
!/.grip
!/.minttyrc
!/.railsrc
!/.rvm
!/.ssh
!/.tmux.conf
!/.vagrant.d
!/.vim
!/lib
!/opt

# Drush - config, not cache
/.drush/cache

# Elinks
/.elinks/bookmarks
/.elinks/cookies
/.elinks/globhist
/.elinks/searchhist

# Grip - config only, not cache & access token
/.grip/cache*
/.grip/settings-local.py

# RVM - after_cd hook only
/.rvm/*
!/.rvm/hooks/
/.rvm/hooks/*
!/.rvm/hooks/after_cd_record_bash_lastdirectory

# SSH - config & public keys only, but ignore per-account public keys
/.ssh/*
!/.ssh/config
!/.ssh/*.pub
/.ssh/google_compute_engine.pub
/.ssh/id_rsa.pub

!/.sudo_as_admin_successful

# Vagrant
/.vagrant.d/*
!/.vagrant.d/provision-*.sh
!/.vagrant.d/Vagrantfile

# Vim - ignore history
/.vim/.netrwhist

# Vim plugins - ignore tags files
/.vim/bundle/*/doc/tags

# Python compiled files
*.pyc

# This is required until all accounts have updated to the new structure
!/bin
/bin/*
!/bin/cfg-update

# cPanel files
.ea-php-cli.cache
