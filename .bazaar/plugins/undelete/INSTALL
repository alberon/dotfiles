How to install 'bzr-undelete':
'bzr-undelete' is a bzr pluging so you can install it in your
plugins directory.

    $ mv ~/bzr-undelete ~/.bazaar/plugins/undelete

You may have to create the plugins directory first.
Also, note that the plugin should be placed as 'undelete' and NOT
'bzr-undelete' in the plugins directory.

